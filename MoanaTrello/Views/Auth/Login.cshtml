@using MoanaTrello.Models.Helpers


@model LoginRequest

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Bejelentkezés";
}


<div class="container w-50">
    <h1>Bejelentkezés</h1>

    @using (Html.BeginForm("Login", "Auth", FormMethod.Post))
    {
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label class="control-label">Email</label>
            <div class="col-md-12">
                @Html.EditorFor(m => m.Email, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
            </div>

            <label class="control-label">Jelszó</label>
            <div class="col-md-12">
                @Html.EditorFor(m => m.Password, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Password, "", new { @class = "text-danger" })
            </div>
            <br />
            <div class="row">
                <div class="col-6">
                    <button class="btn btn-block btn-primary" type="submit">Bejelentkezés</button>
                </div>
                <div class="col-6" >
                    <a class="btn btn-block btn-success" href="@Url.Action("Register")">Regisztráció</a>
                </div>
                </div>
            </div>

    }
</div>